<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Thêm activity GuideActivity -->
        <activity android:name=".GuideActivity" />

        <!-- Thêm activity TopicListActivity -->
        <activity android:name=".TopicListActivity"/>

        <!-- Thêm activity TopicDetailActivity -->
        <activity android:name=".TopicDetailActivity"/>

        <!-- Thêm cấu hình cho VocabDetailActivity -->
        <activity
            android:name=".VocabDetailActivity"
            android:configChanges="orientation|screenSize"
            android:exported="false" />

        <!-- Thêm cấu hình cho MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
