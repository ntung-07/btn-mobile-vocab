<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobile_vocab_project">

    <!-- Thêm quyền truy cập Internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Thêm activity GuideActivity -->
        <activity android:name=".vocab.GuideActivity" />

        <!-- Thêm activity TopicListActivity -->
        <activity android:name=".vocab.TopicListActivity"/>

        <!-- Thêm activity TopicDetailActivity -->
        <activity android:name=".vocab.TopicDetailActivity"/>

        <!-- Thêm cấu hình cho VocabDetailActivity -->
        <activity
            android:name=".vocab.VocabDetailActivity"
            android:configChanges="orientation|screenSize"
            android:exported="false" />


            android:windowSoftInputMode="adjustResize" />

        <!-- Thêm cấu hình cho MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".QuizActivity"/>
    </application>

</manifest>
