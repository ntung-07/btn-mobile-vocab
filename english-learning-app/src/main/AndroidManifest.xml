<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobile_vocab_project">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".VocabApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustResize">

        <!-- Broadcast Receiver for notifications -->
        <receiver android:name=".NotificationReceiver"
            android:exported="false" />

        <!-- Activities -->
        <activity android:name=".GuideActivity" />
        <activity android:name=".topics.TopicListActivity" />
        <activity android:name=".topics.TopicDetailActivity" />
        <activity android:name=".QuizActivity"/>
        <activity android:name=".SettingsActivity"/>
        <activity android:name=".AccountFragment" />
        <activity android:name=".home.WordDetailActivity" />

        <activity
            android:name=".vocab.VocabDetailActivity"
            android:configChanges="orientation|screenSize"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
